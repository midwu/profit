# Shop data (private / server-specific — don't push your scrape)
shop_data.csv

# Generated output — regenerated by running the scripts, no need to track
profitable_trades.csv
profitable_trades_readable.txt
best_trade_per_item.csv
top_trades_readable.txt
items_can_flip.txt
items_cannot_flip.txt
self_flip_opportunities.csv
self_flip_opportunities.txt

# NOTE: ignore_items.txt / ignore_owners.txt / ignore_warps.txt are NOT
# listed here on purpose — those are hand-maintained config, not generated
# output, so they stay tracked.

# Editor / OS
.idea/
__pycache__/
*.pyc
.DS_Store